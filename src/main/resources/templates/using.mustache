<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <!-- css link -->
    <link rel="stylesheet" href="/css/font.css">
    <title>어따히지</title>

</head>
<body>
<center>

    <img src="images/logo.png" />
    <h1>서비스를 이용 중입니다 :)</h1>

</center>
<script>
    async function isUsing() {
        return new Promise(resolve => {
            resolve(true);
        })
    }
    let timer = setTimeout(async function request() {
        let parking = await isUsing();
        if (parking === true){
            location.href="/payment";
        } else {
            timer = setTimeout(request, 2000);
        }
    }, 2000);
</script>
</body>
</html>


